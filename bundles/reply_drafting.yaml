bundle:
  name: strapped-reply-drafting
  version: 1.0.0
  description: >
    Reply drafting bundle — tools for composing professional
    scheduling replies and sending them from the Vela shared mailbox.
  tags: ["strapped", "email", "reply", "drafting"]

tools:
  - name: draft_reply
    description: Compose a tone-appropriate scheduling reply email
    module_class: tools.reply_tools.DraftReplyTool

  - name: send_reply
    description: Send a reply from the Vela shared mailbox via Graph API
    module_class: tools.reply_tools.SendReplyTool
