# ──────────────────────────────────────────────────────────────────────────────
# Vela-Law Dependencies
# ──────────────────────────────────────────────────────────────────────────────

# Microsoft Amplifier (core + foundation)
amplifier-core @ git+https://github.com/microsoft/amplifier-core@main
amplifier-foundation @ git+https://github.com/microsoft/amplifier-foundation@main

# Azure Functions runtime
azure-functions>=1.21.0

# Azure Identity & Graph
azure-identity>=1.19.0
msgraph-sdk>=1.12.0

# Azure Table Storage
azure-data-tables>=12.6.0

# Azure OpenAI
openai>=1.58.0

# Data models
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-dateutil>=2.9.0
pytz>=2024.2
python-dotenv>=1.0.1

# Streamlit dashboard (optional — install separately for local dev)
# streamlit>=1.41.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
